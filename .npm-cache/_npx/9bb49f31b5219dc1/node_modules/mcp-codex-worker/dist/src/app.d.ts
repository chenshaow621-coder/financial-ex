import { TaskManager } from './task/task-manager.js';
export declare class CodexWorkerApp {
    private readonly runtime;
    private taskManager;
    private registry;
    private fileWriter;
    private persistenceRoot;
    private evictionInterval?;
    initialize(): Promise<void>;
    getTaskManager(): TaskManager;
    shutdown(): Promise<void>;
    readResource(uri: string): Promise<{
        mimeType: string;
        text: string;
    }>;
    callTool(name: string, args: unknown): Promise<string>;
    /**
     * Like callTool but threads a progress reporter through wait-task.
     * Claude Code sends a progressToken via _meta — we push progress
     * notifications during the polling loop so the client shows live status.
     */
    callToolWithProgress(name: string, args: unknown, reportProgress?: (progress: number, total: number, message?: string) => Promise<void>): Promise<string>;
    private handleSpawnTask;
    private handleWaitTask;
    private handleRespondTask;
    private handleMessageTask;
    private handleCancelTask;
}
