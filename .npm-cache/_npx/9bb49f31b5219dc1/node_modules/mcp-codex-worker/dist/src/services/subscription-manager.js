export class SubscriptionManager {
    uris = new Set();
    subscribe(uri) {
        this.uris.add(uri);
    }
    unsubscribe(uri) {
        this.uris.delete(uri);
    }
    isSubscribed(uri) {
        return this.uris.has(uri);
    }
    getSubscribed() {
        return [...this.uris];
    }
    getMatchingSubscriptions(taskId) {
        const matches = [];
        for (const uri of this.uris) {
            if (uri === 'task:///all') {
                matches.push(uri);
            }
            else if (uri === `task:///${taskId}` || uri.startsWith(`task:///${taskId}/`)) {
                matches.push(uri);
            }
        }
        return matches;
    }
}
//# sourceMappingURL=subscription-manager.js.map