export interface ModelInfo {
    id: string;
    model: string;
    displayName: string;
    hidden: boolean;
    upgrade: string | null;
    isDefault: boolean;
}
export interface ModelCatalog {
    visibleModels: ModelInfo[];
    byId: Map<string, ModelInfo>;
    upgradeAliases: Map<string, string>;
    defaultModelId?: string | undefined;
}
export interface ModelResolution {
    requested: string;
    resolved: string;
    remappedFrom?: string | undefined;
}
export declare function buildModelCatalog(models: ModelInfo[]): ModelCatalog;
export declare function describeAllowedModels(catalog: ModelCatalog): string;
export declare function resolveModel(catalog: ModelCatalog, requestedModel?: string | undefined): ModelResolution;
